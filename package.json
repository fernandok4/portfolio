{
  "name": "kanashaworks-landing-tests",
  "version": "1.0.0",
  "description": "Playwright tests for KanashaWorks corporate landing page",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
